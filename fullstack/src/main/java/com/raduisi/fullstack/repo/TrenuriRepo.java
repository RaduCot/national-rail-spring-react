package com.raduisi.fullstack.repo;

import com.raduisi.fullstack.model.Trenuri;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TrenuriRepo extends JpaRepository<Trenuri, Long> {
}
